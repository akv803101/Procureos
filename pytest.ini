[pytest]
# asyncio_mode=auto lets us write `async def test_...` without per-test markers.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Make the project root importable (core, services, ...) when running from root.
pythonpath = .
testpaths = tests
